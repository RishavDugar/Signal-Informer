yfinance>=0.2.40,<1.0.0
pandas>=2.0.0
numpy>=1.24.0
APScheduler>=3.10.4
requests>=2.31.0
python-dotenv>=1.0.0
tenacity>=8.2.0
pytz>=2024.1
pywhatkit>=5.4      # ONLY for legacy WHATSAPP_BACKEND=pywhatkit; the default
                    # "bridge" backend uses the Node service in
                    # notifications/whatsapp_bridge/ (run npm install there).
flask>=3.0          # web dashboard (run_ui.py)
